<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wocalage.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".aidl.AIDLActivity"></activity>
        <activity android:name=".cache.CacheActivity"></activity>
        <activity android:name=".database.DatabaseActivity"></activity>
        <activity android:name=".dynamicload.DynamicLoadActivity"></activity>
        <activity android:name=".framework.FrameworkActivity"></activity>
        <activity android:name=".jni.JNIActivity"></activity>
        <activity android:name=".net.NetActivity"></activity>
        <activity android:name=".ui.UIActivity"></activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>